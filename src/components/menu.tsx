"use client";

import gsap from "gsap";
import { useGSAP } from "@gsap/react";

const Menu = () => {
  const menu_hot_drink: string[] = [
    "espresso ",
    "capochino",
    "turkish coffee",
    "late",
    "amreicano",
    "tea",
    "caramel makiato",
    "macha",
  ];
  return (
    <section className="menu w-full h-fit">
      <div></div>
    </section>
  );
};
